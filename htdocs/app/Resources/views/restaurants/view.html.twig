{% extends 'layout/base.html.twig' %}

{% block title %}{{ 'restaurant.view.page_title'|trans }}{% endblock %}

{% block page_header %}
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            {{ restaurant.name|upper }}
            <small>{{ 'header.restaurant.view.optional'|trans }}</small>
        </h1>
        {{ include('layout/parts/breadcrumb.html.twig',
            {'pages': [
                {
                    'title': restaurant.name,
                }
            ]
            })
        }}
    </section>
{% endblock %}

{% block content %}
    {#<div class="col-md-2">
        <div class="box box-primary">
            <div class="box-header">
                <h2 class="text-center">{{ 'restaurant.view.infos'|trans }}</h2>
            </div>
            <div class="box-body">
                <span class="text-left"><b>{{ 'restaurant.edit.users'|trans }}</b></span>
                <ul class="list-group list-group-unbordered">
                    {% for user in restaurant.users if user.accessRole == 'ROLE_USERS' %}
                        <li class="list-group-item"><a href="{{ path('user_profile', {'id': user.id}) }}" target="_blank">{{ user.username }}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>#}

    {{ include('widgets/annual_budget_list.html.twig', {'annualBudgets': annualBudgets|default([]), 'colWidth': 12, 'showEdit': false}) }}
{% endblock %}