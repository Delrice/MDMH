{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="callout callout-{{ label }}">
            {{ message|trans }}
        </div>
    {% endfor %}
{% endfor %}