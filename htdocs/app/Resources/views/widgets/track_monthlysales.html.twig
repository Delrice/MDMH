<div class="box box-info box-body">
    {# FIRST 4 MONTHS #}
    <table class="table table-bordered text-center">
        <tr class="text-uppercase bg-gray-active">
            <th style="width: 90px">{{ 'track.monthlysales.year'|trans }}</th>
            <th colspan="5">{{ 'month-1'|trans() }}</th>
            <th colspan="5">{{ 'month-2'|trans() }}</th>
            <th colspan="5">{{ 'month-3'|trans() }}</th>
            <th colspan="5">{{ 'month-4'|trans() }}</th>
        </tr>
        <tr class="text-uppercase bg-gray">
            <th style="width: 90px"></th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
        </tr>

        {% for year in elements.year.min..elements.year.max %}
            <tr>
                <td>{{ year }}</td>
                {% for month in 1..4 %}
                    {% if elements.items[month][year]|default(false) %}
                        <td>{{ elements.items[month][year]['amount']|number_format(0, '.', ' ') }} &euro;</td>
                        <td>{{ elements.items[month][year]['ratio_year1_year']|number_format(2, '.', ' ') }} %</td>
                        <td>{{ elements.items[month][year]['SE']|number_format(2, '.', ' ') }} %</td>
                        <td>{{ elements.items[month][year]['ratio_year_SE']|number_format(2, '.', ' ') }}</td>
                        <td>{{ elements.items[month][year]['rank'] }}</td>
                    {% else %}
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                    {% endif %}
                {% endfor %}
            </tr>
        {% endfor %}
    </table>

    {# SECOND 4 MONTHS #}
    <table class="table table-bordered text-center">
        <tr class="text-uppercase bg-gray-active">
            <th style="width: 90px">{{ 'track.monthlysales.year'|trans }}</th>
            <th colspan="5">{{ 'month-5'|trans() }}</th>
            <th colspan="5">{{ 'month-6'|trans() }}</th>
            <th colspan="5">{{ 'month-7'|trans() }}</th>
            <th colspan="5">{{ 'month-8'|trans() }}</th>
        </tr>
        <tr class="text-uppercase bg-gray">
            <th style="width: 90px"></th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
        </tr>

        {% for year in elements.year.min..elements.year.max %}
            <tr>
                <td>{{ year }}</td>
                {% for month in 5..8 %}
                    {% if elements.items[month][year]|default(false) %}
                        <td>{{ elements.items[month][year]['amount']|number_format(0, '.', ' ') }} &euro;</td>
                        <td>{{ elements.items[month][year]['ratio_year1_year']|number_format(2, '.', ' ') }} %</td>
                        <td>{{ elements.items[month][year]['SE']|number_format(2, '.', ' ') }} %</td>
                        <td>{{ elements.items[month][year]['ratio_year_SE']|number_format(2, '.', ' ') }}</td>
                        <td>{{ elements.items[month][year]['rank'] }}</td>
                    {% else %}
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                    {% endif %}
                {% endfor %}
            </tr>
        {% endfor %}
    </table>

    {# THIRD 4 MONTHS #}
    <table class="table table-bordered text-center">
        <tr class="text-uppercase bg-gray-active">
            <th style="width: 90px">{{ 'track.monthlysales.year'|trans }}</th>
            <th colspan="5">{{ 'month-9'|trans() }}</th>
            <th colspan="5">{{ 'month-10'|trans() }}</th>
            <th colspan="5">{{ 'month-11'|trans() }}</th>
            <th colspan="5">{{ 'month-12'|trans() }}</th>
        </tr>
        <tr class="text-uppercase bg-gray">
            <th style="width: 90px"></th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
            <th>{{ 'track.monthlysales.amount'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year1_year'|trans() }}</th>
            <th>{{ 'track.monthlysales.SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.ratio_year_SE'|trans() }}</th>
            <th>{{ 'track.monthlysales.rank'|trans() }}</th>
        </tr>

        {% for year in elements.year.min..elements.year.max %}
            <tr>
                <td>{{ year }}</td>
                {% for month in 9..12 %}
                    {% if elements.items[month][year]|default(false) %}
                        <td>{{ elements.items[month][year]['amount']|number_format(0, '.', ' ') }} &euro;</td>
                        <td>{{ elements.items[month][year]['ratio_year1_year']|number_format(2, '.', ' ') }} %</td>
                        <td>{{ elements.items[month][year]['SE']|number_format(2, '.', ' ') }} %</td>
                        <td>{{ elements.items[month][year]['ratio_year_SE']|number_format(2, '.', ' ') }}</td>
                        <td>{{ elements.items[month][year]['rank'] }}</td>
                    {% else %}
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                    {% endif %}
                {% endfor %}
            </tr>
        {% endfor %}
    </table>

</div>