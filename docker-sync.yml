version: "2"

options:
  verbose: false
syncs:
  mdmh-code-native-osx-sync: # tip: add -sync and you keep consistent names as a convention
    src: './htdocs'
    sync_excludes: ['./var/', './htdocs/var/', 'var/', 'htdocs/var/']

# /usr/local/bin/docker-sync-stack start